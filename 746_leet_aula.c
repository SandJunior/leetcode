#include <stdio.h>
#include <stdlib.h>

int minCostClimbingStairs(int* cost, int costSize){
    int maiorCost;
    int minCost;
    //int cost2[i];

    //printf("costsize: ");
    //scanf("%d", &costSize);

    for (int i=0; i<costSize; i++){
        minCost[i] = (int *)malloc(sizeof())
        cost2[i] = (int *)malloc(costSize * sizeof(int));

    }

    for (int i=0; i<costSize; i++){
        if(cost < minCost)
            minCost = cost;

        else
            cost += minCost; 
    }

    








    return minCost;
}
